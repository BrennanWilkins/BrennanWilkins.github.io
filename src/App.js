import NavBar from './components/NavBar/NavBar';

const App = () => {
  const scrollHandler = section => {

  };
  
  return (
    <div>
      <NavBar scroll={scrollHandler} />
    </div>
  );
};

export default App;
